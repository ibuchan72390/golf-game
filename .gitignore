node_modules/
dist/
.superpowers/
.worktrees/
test-results/
playwright-report/
.playwright-mcp/
# Local multiplayer config (real backend). .env.example is the committed template.
.env
.env.local
.env.*.local
# One-time bootstrap helper holding real credentials — never commit.
seed-secrets.sh

# Terraform
**/.terraform/*
*.tfstate
*.tfstate.*
crash.log
crash.*.log
*.tfvars.local
override.tf
override.tf.json
.terraformrc
terraform.rc
